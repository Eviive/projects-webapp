name: Pull Request

on:
  pull_request:

jobs:
  prettier:
    name: Prettier
    uses: eviive/actions/.github/workflows/test/validate-format-prettier.yaml@chore/reusable-workflows

  eslint:
    name: ESLint
    uses: eviive/actions/.github/workflows/test/validate-lint-eslint.yaml@chore/reusable-workflows
