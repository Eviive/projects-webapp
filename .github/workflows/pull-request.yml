name: Pull Request

on:
  pull_request:

jobs:
  prettier:
    uses: eviive/actions/.github/workflows/validate-format-prettier.yaml@chore/reusable-workflows

  eslint:
    uses: eviive/actions/.github/workflows/validate-lint-eslint.yaml@chore/reusable-workflows
